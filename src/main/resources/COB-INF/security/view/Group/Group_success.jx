<?xml version="1.0"?>
<jx:template xmlns:jx="http://apache.org/cocoon/templates/jx/1.0">
    <jx:forEach select="#{language}" xmlns:jx="http://apache.org/cocoon/templates/jx/1.0">
            <language id="#{id}">
                <name>#{name}</name>
                <languageCode>#{languageCode}</languageCode>
                <charset>#{charset}</charset>
            </language>
        </jx:forEach>
   <!--z/яяя -->
</jx:template>